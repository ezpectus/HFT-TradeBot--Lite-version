# ─────────────────────────────────────────────────────────────────────────────
# Production environment variables — copy to .env.prod and fill in secrets
# ─────────────────────────────────────────────────────────────────────────────
# NEVER commit .env.prod to version control!

# ── Exchange API Keys ──
BINANCE_API_KEY=your_binance_api_key_here
BINANCE_API_SECRET=your_binance_api_secret_here

OKX_API_KEY=your_okx_api_key_here
OKX_API_SECRET=your_okx_api_secret_here
OKX_PASSPHRASE=your_okx_passphrase_here

BYBIT_API_KEY=your_bybit_api_key_here
BYBIT_API_SECRET=your_bybit_api_secret_here

# ── FIX Gateway (optional) ──
FIX_USERNAME=your_fix_username
FIX_PASSWORD=your_fix_password

# ── PostgreSQL ──
POSTGRES_DB=trading
POSTGRES_USER=trader
POSTGRES_PASSWORD=change_me_to_a_secure_password
POSTGRES_DSN=postgresql://trader:change_me_to_a_secure_password@postgres:5432/trading

# ── Redis ──
REDIS_URL=redis://redis:6379/0

# ── Grafana ──
GRAFANA_USER=admin
GRAFANA_PASSWORD=change_me_to_a_secure_password
# Used by Grafana PostgreSQL datasource (same as POSTGRES_PASSWORD)
GF_POSTGRES_PASSWORD=change_me_to_a_secure_password

# ── Web UI (Vite build-time args) ──
# These are inlined into the JS bundle at Docker build time.
# Override when building for a different host/domain.
VITE_WS_EXCHANGE=ws://localhost:8765
VITE_WS_SIGNALS=ws://localhost:8766

# ── Exchange Mode ──
# simulator = use local exchange simulator (safe, no real orders)
# production = use real exchange APIs (REAL MONEY AT RISK)
EXCHANGE_MODE=simulator

# ── AI Signal Bot ──
OPENAI_API_KEY=your_openai_api_key_here
